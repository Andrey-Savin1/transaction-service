spring:
  flyway:
    enabled: false # Отключаем Flyway для этого профиля
  datasource:
    driver-class-name: org.apache.shardingsphere.driver.ShardingSphereDriver
    url: jdbc:shardingsphere:classpath:sharding-test.yaml